
spring:
  application:
    name: config-single-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/veeHsing/spring-cloud-conf-repo
          username: veeHsing
          password: zhangwei2009x
          default-label: master
          search-paths: config